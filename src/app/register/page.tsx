"use client";
import { useState } from "react";
import Link from "next/link";
import { PublicNav } from "@/components/layout/PublicNav";
import { apiFetch, setToken } from "@/lib/api";
import type { ApiSession } from "@/lib/types";
export default function RegisterPage(){const [role,setRole]=useState("user"); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault(); const form=new FormData(e.currentTarget); const session=await apiFetch<ApiSession>("/auth/register",{method:"POST",body:JSON.stringify({email:form.get("email"),displayName:form.get("displayName"),password:form.get("password"),role})}); setToken(session.accessToken); window.location.href="/dashboard";} return <div className="page-shell"><PublicNav/><main className="container-wide flex min-h-[calc(100vh-80px)] items-center justify-center py-12"><form onSubmit={submit} className="panel w-full max-w-lg p-8"><h1 className="text-3xl font-black uppercase">Create Account</h1><p className="mt-2 text-white/60">Progressive onboarding: email first, verification/payment details later.</p><input name="displayName" placeholder="Player name" className="mt-6 w-full rounded-xl border border-white/10 bg-black/30 p-3 outline-none" required/><input name="email" type="email" placeholder="Email" className="mt-4 w-full rounded-xl border border-white/10 bg-black/30 p-3 outline-none" required/><input name="password" type="password" placeholder="Password" className="mt-4 w-full rounded-xl border border-white/10 bg-black/30 p-3 outline-none" required/><div className="mt-4 grid grid-cols-4 gap-2">{["user","affiliate","sponsor","admin"].map(r=><button type="button" key={r} onClick={()=>setRole(r)} className={`rounded-lg px-3 py-2 text-xs font-bold uppercase ${role===r?"bg-madness-red":"bg-white/5"}`}>{r}</button>)}</div><button className="btn-primary mt-6 w-full">Claim Free Tokens</button><p className="mt-4 text-sm text-white/55">Already have account? <Link href="/login" className="text-madness-red">Login</Link></p></form></main></div>}
