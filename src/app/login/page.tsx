"use client";
import { useState } from "react";
import Link from "next/link";
import { PublicNav } from "@/components/layout/PublicNav";
import { apiFetch, setToken } from "@/lib/api";
import type { ApiSession } from "@/lib/types";

export default function LoginPage(){const [email,setEmail]=useState("player@fantasymmadness.com"); const [password,setPassword]=useState("ChangeMe123!"); const [error,setError]=useState(""); async function submit(e:React.FormEvent){e.preventDefault(); setError(""); try{const session=await apiFetch<ApiSession>("/auth/login",{method:"POST",body:JSON.stringify({email,password})}); setToken(session.accessToken); window.location.href="/dashboard";}catch(err:any){setError(err.message)}} return <div className="page-shell"><PublicNav/><main className="container-wide flex min-h-[calc(100vh-80px)] items-center justify-center py-12"><form onSubmit={submit} className="panel w-full max-w-md p-8"><h1 className="text-3xl font-black uppercase">Login</h1><p className="mt-2 text-white/60">Use demo account or your real user credentials.</p>{error && <p className="mt-4 rounded bg-red-500/20 p-3 text-sm text-red-100">{error}</p>}<label className="mt-6 block text-sm font-bold">Email</label><input value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 w-full rounded-xl border border-white/10 bg-black/30 p-3 outline-none"/><label className="mt-4 block text-sm font-bold">Password</label><input value={password} onChange={e=>setPassword(e.target.value)} type="password" className="mt-2 w-full rounded-xl border border-white/10 bg-black/30 p-3 outline-none"/><button className="btn-primary mt-6 w-full">Login</button><p className="mt-4 text-sm text-white/55">Need an account? <Link href="/register" className="text-madness-red">Sign up</Link></p></form></main></div>}
